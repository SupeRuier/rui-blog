# 撰写标准（Writing Principles）

本文件是本站**内容层面的单一事实来源**：文章写什么、写成什么样、哪些信息必须以何种形式出现，都以这里为准。

- 适用范围：`content/posts/*.md`、`content/site.json` 的文案部分。
- 不适用范围：构建脚本、模板、样式、部署流程 —— 那些看 `README.md`。
- 版本：**V1.3**（在 V1.2 基础上修订 §2 首页排序，见文末「修订记录」）。修改本文件时请递增版本号，并说明改了什么、为什么改。
- AI 协作：任何自动改写、生成或评审文章的流程，都必须先读本文件（`AGENTS.md` 里有强制约定）。

---

## 1. 博客定位与内容原则

这是一个用于**长期记录、整理和沉淀个人技术认知的技术博客**。

博客主要面向具有一定技术背景的读者。读者可能熟悉具体技术但缺乏完整的宏观地图，也可能已经理解整体架构，但对部分细分领域并不熟悉。

因此，文章希望同时提供两种价值：

- **建立 Big Picture**：整理 Architecture、Taxonomy、Landscape 和不同技术之间的关系，帮助读者建立完整的 mental model。
- **提供足够的技术深度**：在整体框架之下继续展开关键机制、技术细节、案例和 evidence。

博客整体遵循**少而重**的原则，不追求文章数量和更新频率。每篇文章尽量形成一个相对完整的认知单元，主要记录：

- 小型研究 / 技术项目；
- Survey / Landscape；
- 值得系统研究的技术问题；
- 架构与系统设计；
- 阶段性的技术总结；
- 持续更新的领域前沿进展。

文章通常不会围绕单篇论文或单一技术点展开。论文、代码仓库和具体技术实现更多作为 evidence / case study，用于支撑一个更完整的问题或技术主题。

核心目标是：

> **把复杂、碎片化的技术信息整理成清晰、有结构、有深度，并且能够持续演进的个人技术认知。**

---

## 2. 首页原则

首页保持极简。

只需要：

- 少量个人信息；
- Email / GitHub 等必要入口；
- 所有文章按**阅读顺序**排列，并显示顺序编号。

暂时不增加额外的内容分类、Featured、Tags 或知识图谱。

### 为什么不是按时间排列（V1.3 修订）

V1.2 原文要求「按时间从新到旧排列」。实际落地时发现它不成立，因此改为编辑顺序，理由有两条：

1. **本站不是信息流，是认知地图。** 「少而重」「每篇是完整认知单元」「先建立 Big Picture」描述的是一门**有依赖关系**的课程：先有数据，才谈模型；先理解 System 1 / System 2 的分工，才读得懂 Harness。反向时间序回答的是「什么最新」，而本站要回答的是「应该先读哪篇」——这两个顺序经常相反。
2. **这批内容没有真实的时间分布。** 站内 15 篇文章是同一次集中构建的（14 篇首次上线于同一天），时间序在这个集合上没有任何区分度：排序实际由编号决定，日期只是给它套了一层时间的外壳。

因此拆成两个字段，各管一件事：

| 字段 | 回答的问题 | 作用 |
| --- | --- | --- |
| `order` | 读者应该先读哪篇 | 决定首页顺序，数字越大越靠前；`pinned` 最优先 |
| `date` | 这篇有多新 | **占位，先不填**；填了只在文章页页眉显示，不参与排序 |

`date` 这一行留在每篇的 front-matter 里，但**先不填值**。理由和排序换掉是同一个：这批内容是同一次集中构建的，写不出有意义的时间差——与其填一排同天的日期，不如留空。

字段本身不删。技术领域半年就变天，读者需要知道一篇判断写于何时，「持续更新的领域前沿进展」这类内容尤其依赖时间；有的文章（比如对一个新发布系统的评估）将来确实值得标日期。保留空占位，需要时填一篇即可，不用再动代码。

---

## 3. 单篇文章设计原则

文章整体保持**中等偏深**的技术深度，但通过 Progressive Disclosure 兼顾不同的阅读方式：

- 快速浏览时，可以通过 TL;DR、Overview Figure 和核心结论获得主要信息；
- 完整阅读时，可以建立较完整的问题框架和 mental model；
- 深入阅读时，可以继续进入 mechanism、implementation、算法、案例、evidence 和 references。

因此，文章本身可以很深，但**核心思想必须能够被快速提取**。

不同文章不需要套用完全一致的正文模板，但重要文章尽量包含以下几个核心元素。

### 3.1 TL;DR

用较短篇幅直接说明：

- 这篇文章讨论什么问题；
- 最重要的结论是什么；
- 读者可以从中获得什么新的信息或理解。

TL;DR 不只是 Abstract 的缩写，而应该让读者即使暂时不继续阅读正文，也能够获得文章最重要的信息。

---

### 3.2 Overview Figure

每篇重要文章尽量在前部提供一张能够概括全文的核心图。

根据文章内容，它可以是：

- Architecture；
- Taxonomy；
- Landscape；
- Roadmap；
- Workflow；
- System Overview；
- Problem Map。

Overview Figure 不是普通配图，而是文章的**认知入口**。它应该尽可能：

- 给出整个问题的 Big Picture；
- 表达关键模块、概念或类别之间的关系；
- 标明文章关注的范围；
- 帮助读者在进入细节之前建立整体 mental model。

核心原则是：

> **先让读者看到森林，再进入树木。**

TL;DR 用文字压缩全文，Overview Figure 用视觉压缩全文。

---

### 3.3 正文

正文按照最适合当前问题的逻辑自由展开，不要求固定使用某一种模板。

组织内容时，不按照自己实际的研究和学习过程写流水账，而应该按照**读者建立 mental model 的顺序**重新组织。

通常先建立整体问题和结构，再逐渐进入具体方法、机制、案例和技术细节。

文章中的论文、项目、实验结果和代码实现应该作为 evidence 支撑整体分析，而不是简单进行资料罗列。

---

### 3.4 My Take

文章需要明确区分已有事实和个人认知，但不需要机械地将两者拆成独立章节。

根据个人观点的重要程度，采用两种方式表达。

#### 重要个人观点：`My Take`

对于真正重要的 synthesis、abstraction、分类方式或技术判断，使用固定的 `My Take` 模块，并进行明显的视觉强调。

例如：

> **My Take**
>
> I find it more useful to distinguish embodied self-improvement methods by whether they enable **capability refinement** or **capability acquisition**, rather than simply by whether they use SFT or RL.

`My Take` 代表：

> **这是在已有事实和 evidence 基础上进一步形成的重要个人认知。**

使用需要保持克制，只强调真正值得读者记住的判断。

#### 普通个人观点：语言归因

一般性的分析、推断和个人理解直接通过语言表达，例如：

> From this perspective, I view...

> In my view...

> A more useful way to understand this is...

> In this article, I use the following taxonomy...

核心要求是：

> **不要让个人 inference 看起来像 established fact。**

因此：

**重要个人观点 → `My Take` 高亮。**

**普通个人观点 → 语言归因。**

---

### 3.5 Synthesis & Discussion

文章不能只停留在资料整理。

在完成事实、方法、案例和 evidence 的组织之后，应尽可能进一步回答：

- 这些信息放在一起意味着什么？
- 不同方法之间真正关键的区别是什么？
- 当前方法解决了什么，又没有解决什么？
- 是否可以形成更好的 abstraction 或 taxonomy？
- 还有哪些值得继续思考的问题？

这些内容可以自然分布在正文和 `My Take` 中，也可以在文章后部集中形成 Discussion，不强制采用固定形式。

核心是从：

> **Information → Organization → Comparison**

进一步走向：

> **Synthesis → Interpretation → Open Questions**

---

### 3.6 References

对于论文、项目、数据、实验结果以及重要外部事实，提供清晰、可追溯的来源。

References 不只是形式上的引用列表，也用于明确区分：

- 外部已有事实；
- 他人的观点和结论；
- 自己基于这些材料形成的分析与判断。

---

## 4. 核心原则总结

整个博客最终遵循几个简单原则：

> **少而重，而不是追求数量。**

> **先建立 Big Picture，再进入技术细节。**

> **TL;DR 用文字压缩全文，Overview Figure 用视觉压缩全文。**

> **文章可以很深，但核心认知必须能够被快速提取。**

> **事实需要 evidence，个人观点需要明确归因。**

> **真正重要的个人 synthesis，用 `My Take` 明确标记。**

> **不止整理信息，还要形成自己的结构、解释和认知。**

---

## 5. 与当前实现的对照

原则与实现的落地状态。**写作前先看这里**：标「待补」的还写不出来，标「已支持」的可以直接用。

### 已支持，可以直接用

| 原则要求 | 实现方式 |
| --- | --- |
| 首页按阅读顺序排列（§2） | front-matter 的 `order` 决定排序（`pinned` → `order` 从大到小 → 同级按 slug）；数字可留空档 |
| `date` 时间标注（可选） | 每篇 front-matter 留空占位；填 `YYYY-MM-DD` 后会自动接在文章页页眉分类行后面，卡片上不显示。**当前 15 篇均为空** |
| TL;DR | 文首写 `::: tldr`，标签由构建自动生成，内部支持完整 Markdown（段落、列表） |
| `My Take` | 文中写 `::: my-take`，标签由构建自动生成，内部支持完整 Markdown |
| References 与引用归因 | 正文写 `[@N]`、文末写 `## 参考文献 {#references}`，构建自动补锚点与跳转；`validate()` 会检查悬空引用 |
| Overview Figure | 直接写 `<figure>` HTML（参见 `agent-robo-rsi.md`、`embodied-industry-frontier.md`） |

### 待补，现在还不能用

| 原则要求 | 现状 | 缺什么 |
| --- | --- | --- |
| 首页 Email / GitHub 等必要入口 | `templates/index.html` 与 `content/site.json` 均无联系字段 | 加字段 + 模板槽位 |
| 少量个人信息 | 仅站点标题 `Rui`，无作者介绍 | 加 About 段落或页面 |
| Overview Figure 的统一视觉语言 | 只有 `agent-robo-rsi.md` 一套自绘 SVG 样式（`.rsi-overview` / `.rsi-mini`） | 其余文章要么复用这套，要么另立一套通用 `figure` 样式 |
| `::: note` 的语义 | 目前渲染成 `.draft-note`，被用来标「框架草稿」 | 与 TL;DR 的定位不同，暂时保留；若草稿态要长期存在，建议改名或另立指令 |

> 注：`tools/build.mjs` 遇到未注册的 `::: xxx` 会**直接构建失败**，不是静默忽略。新增指令必须先改代码，再在文章里用。

### 不适用完整文章模板的一类

`section: references` 的**方法文献目录**（目前 3 篇）不套 §3 的模板：这类文章以文献罗列与字段化记录为主，讨论性弱，因此**不要求 TL;DR、Overview Figure 与 `My Take`**。它们仍然要求：引用闭合、字段统一、与主文术语一致，并设置 `back` / `backLabel` 指回主文。放置位置见 §2 与首页底部的「方法文献目录」栏。

---

## 修订记录

| 版本 | 改了什么 | 为什么 |
| --- | --- | --- |
| V1.3 | §2 首页排序由「按时间从新到旧」改为「按阅读顺序（`order`）」；发布日期降为可选的时间标注（`date`），并**全部留空占位**；§5 对照表同步更新 | 本站是同一次集中构建的，时间序在实际数据集上没有区分度；且认知地图要回答的是「先读哪篇」，不是「什么最新」。既然顺序不靠时间，就不该在每篇挂一个同天的日期 |
| V1.2 | — | 本文件纳入仓库时的版本 |

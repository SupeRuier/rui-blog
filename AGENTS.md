# AGENTS.md

本仓库的写作类工作（撰写、改写、评审、翻译 `content/` 下的文章）**必须先读 [`WRITING.md`](./WRITING.md)**，并遵守其中的定位、结构与归因原则。

`WRITING.md` 是内容标准的单一事实来源。本文件只写**仓库操作层面的硬约束**，不复述写作原则 —— 避免两处内容漂移。

## 分工

| 文件 | 作用 |
| --- | --- |
| `WRITING.md` | 文章写成什么样（写作前必读） |
| `README.md` | 仓库怎么跑、front-matter 字段、正文语法、目录结构 |
| 本文件 | 动 `content/`、`templates/`、`tools/` 前的操作约束 |

## 硬约束

1. **块指令是白名单，未知指令直接构建失败。**
   `tools/build.mjs` 遇到未注册的 `::: xxx` 会 `throw`，不会静默降级。
   当前可用：
   - 行内指令（指令体按行内 Markdown 渲染）：`note`、`question`、`method-link`、`date <参数>`
   - 块指令（指令体按完整 Markdown 渲染）：`axis <id>`、`list <class>`、`tldr`、`my-take`

   其中 `tldr`、`my-take` 是 `WRITING.md` 要求的两个核心模块，标题文字由构建自动生成，正文里不要再手写一遍。新增第三类指令时必须先改 `build.mjs` 并在 `styles.css` 加样式。

2. **首页顺序由 `order` 决定，不是发布时间。**
   `order` 数字越大越靠前；`pinned` 最优先且不需要 `order`；`order` 相同时按 slug 兜底，顺序始终确定。
   会显示在首页却没有 `order` 的文章会落到最后并打印警告。
   插入新文章时优先**留空档**（把 `10/20/30` 改成 `15`），不要因为改了顺序就顺手把其他文章的编号全部重排。

3. **`date` 保持空占位，不要填。**
   每篇 front-matter 都有 `date:` 一行，值是空的 —— 这是刻意的，不是遗漏。本站按 `order` 组织，不靠时间。
   不要「顺手补全」日期、不要因为改了内容就填今天、也不要删除这一行（保留占位是为了需要时随时能填）。
   若某一篇确实要标注时效，填 `YYYY-MM-DD` 即可，它只会显示在文章页页眉；格式写错会直接构建失败。

4. **引用格式固定，不要手写序号。**
   正文写 `[@N]`，文末写 `## 参考文献 {#references}`，下面用普通有序列表；构建时自动补 `id="ref-N"` 和跳转。
   不要手写 `[1]` 或 `id="ref-1"`。每篇文章的 `[@N]` 必须与文献条目一一对应，不留悬空引用、不出现未被引用的条目。

5. **文章的 URL 就是文件名。** `content/posts/foo.md` → `/posts/foo.html`。改名即改 URL，不要随意改已有文件名。

6. **`dist/` 是构建产物，禁止手写。** 每次构建整个重建，任何直接放进 `dist/` 的内容都会丢失。

7. **`index: false` 的文章不出现在首页**，只从父文章内链进入。新增文献目录类文章时记得设置 `back` / `backLabel` 指向父文章。

## 每次改动后的检查

```bash
npm run build     # 必须通过；构建失败时 dist/ 会停在上一版
npm run dev       # 本地看效果，改完存盘页面自动刷新
```

构建输出会打印文章数、首页卡片数与警告（`validate()`：slug 重复、`[@N]` 与参考文献不匹配、首页文章缺 `summary` / `cardMeta` / `order`）。**警告必须清零**，不要带着警告提交。

改动 `tools/`、`package.json` 后需要重启 `npm run dev`（构建脚本本身不参与监听）。

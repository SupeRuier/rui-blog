# Rui Blog

一个极简的静态技术博客，用于记录 AI 数据工程、具身智能、Robot Agent 与持续自我改进。

## 添加新文章

1. 复制 `posts/agent-robo-rsi.html` 并修改文件名与正文。
2. 在 `index.html` 的 `.writing` 区域添加对应文章卡片。
3. 提交并推送到 `main`，GitHub Pages 会自动发布。

## 本地预览

```bash
python3 -m http.server 8000
```

打开 `http://localhost:8000`。
